import { LocationDetectionModule } from './location-detection.module';

describe('LocationDetectionModule', () => {
  it('should work', () => {
    expect(new LocationDetectionModule()).toBeDefined();
  });
});
